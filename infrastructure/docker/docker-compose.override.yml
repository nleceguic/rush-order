# Overrides locales — este archivo NO se commitea (está en .gitignore)
# Úsalo para customizar puertos, montar configs extra, etc.
services:
  postgres:
    environment:
      POSTGRES_LOG_STATEMENT: all

  redis:
    command: redis-server --loglevel verbose
